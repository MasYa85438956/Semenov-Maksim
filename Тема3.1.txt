# Файл существует и является обычным файлом
if [ -f "файл.txt" ]; then
    echo "Обычный файл существует"
fi
# Файл доступен для записи
if [ -w "файл.txt" ]; then
    echo "Файл доступен для записи"
fi
# Проверка времени модификации файла
# -nt (newer than) - новее чем
if [ "файл1.txt" -nt "файл2.txt" ]; then
    echo "файл1 новее файла2"
fi
